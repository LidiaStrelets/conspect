function logIConsole(array) {
    console.log('Элементы массива: ');
    for (const element of array) {
        console.log(element);
    }
} 
logIConsole([6, 9, 08, 66]);
logIConsole(['mango', 'kiwi'])